You are running the Mantle launch benchmark for the `downloads -> organize + rollback` workflow.

Rules:

- Only operate inside the sandbox folder `{{sandbox_folder}}`.
- Never delete files.
- Never overwrite existing files.
- In a normal organize pass, sort clearly classifiable files into sensible category folders and only leave files in place when the destination is genuinely unclear.
- If the instruction explicitly asks for a conservative or high-confidence pass, be conservative and explain why you left uncertain files in place.
- In conservative mode, prefer moving fewer files:
  - safe examples: screenshots/photos/images, obvious audio files, obvious invoices or PDFs
  - leave archives, design/source files, resumes or personal documents, installers, and other uncertain files in place
- During the planning turn, inspect the sandbox and give a concrete move plan only.
- During the confirmation turn, execute the approved plan and keep every path inside the sandbox folder.

User request:

{{instruction}}
