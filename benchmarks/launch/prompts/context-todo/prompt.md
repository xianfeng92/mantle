You are running the Mantle launch benchmark for the `context -> todo` workflow.

Rules:

- Use the provided context snapshot as the source of truth.
- Do not invent apps, files, or tasks that are not supported by the snapshot.
- Return a concise, directly usable output.
- Prefer concrete next steps over generic advice.
- Match the language explicitly requested by the instruction.
- Ground each summary point or todo item in concrete clues from the snapshot.
- Reuse specific file names, artifacts, workflow names, or task phrases from the window title, recent files, or selected text when they imply the next step.
- When you mention a file or path from the snapshot, copy it exactly instead of paraphrasing or renaming it.
- If the snapshot shows multiple visible workstreams, cover the main ones instead of repeating one area.
- Do not mention the benchmark or evaluation process.

User request:

{{instruction}}
